# Placeholder Dockerfile to force Zeabur into Docker mode
FROM alpine:latest
CMD ["echo", "This is a placeholder to prevent Static detection. Please configure specific Dockerfile paths in Zeabur Settings."]
